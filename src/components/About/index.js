import React from "react";
import styled from "styled-components";

const AboutContainer = styled.section`
  background: #fff;
`;

export default () => {
  return (
    <AboutContainer>
      <p>From about with love</p>
    </AboutContainer>
  );
};
